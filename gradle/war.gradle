apply plugin: "war"

bootWar {
    mainClassName = "id.tech.cakra.ecfpaymentsvc.EcfpaymentsvcApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
